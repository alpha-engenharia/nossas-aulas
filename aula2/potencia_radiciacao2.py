# pow(x, y) = x^y
# sqrt(x) = raiz quadrada de x

from math import pow, sqrt

a = 2
b = 3

potencia = pow(2, 3) # resulta em 2³
raiz_quad_2 = sqrt(2) # resulta em raiz quadrada de 2

print(potencia, raiz_quad_2)